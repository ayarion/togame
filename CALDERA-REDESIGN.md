# トガメ リデザイン仕様書 — Caldera 適用

元仕様書（Refero Styles の Caldera デザインシステムをトガメに適用する案）を、実際のコードと突き合わせて実装可能な形に落とし込んだもの。

参照元は `https://styles.refero.design/style/fe8cdcf9-c850-4d52-be07-5ad269bf9ebf` だが、この作業環境からは外部サイトへの接続が塞がれているため未確認である。パレットとタイポグラフィの数値は、元仕様書に記載された値をそのまま正とした。

---

## 0. 元仕様書からの変更点と、その理由

先に相違点を並べる。理由なく変えた箇所はない。

| 箇所 | 元仕様書 | 本仕様書 | 理由 |
|---|---|---|---|
| オンボーディング | 5ステップを維持 | 対象から外す | コードに存在しない。あるのは設定画面内の折りたたみ手順書のみ |
| 見出し書体 | Bebas Neue / Anton | 日本語対応書体に差し替え | Bebas Neue と Anton は日本語グリフを持たない。「なんで開いたの？」が描画できない |
| 2色セマンティック | Ember と Sulfur に割当 | Ember と Obsidian輪郭 に割当 | Sulfur は明色地でコントラスト不足。かつ黄色は目的ありの意味に合わない |
| カード内パディング | 40px 固定 | 32px（480px未満は24px） | 390px幅で40pxを両側に取ると本文が262pxまで痩せる |
| 問い詰め画面の地色 | Limestone 基調 | Obsidian 基調 | 後述。この一点が本仕様書の中心 |

---

## 1. スコープ

対象は介入フローの4画面に限る。

| 画面 | 関数 | 内容 |
|---|---|---|
| 問い詰め | `renderIntercept` | なんで開いたの？ 理由4択 |
| 返し | `renderFollowup` | 理由ごとの一言、それでも開く／やめておく |
| 引き返した | `renderResistDone` | 回数、今日の記録 |
| 素通り | `renderGrace` | さっき答えたばかり |

対象外はダッシュボード（`renderDashboard` `renderRecovered` `renderHeatmap` `renderEmpty`）と設定（`renderSettings`）。ただし介入画面だけ世界観が変わると、まとめを見るを押した瞬間に別アプリになる。第2段としてダッシュボードまで広げる前提で、パレットと変数は全画面共通で定義しておく。

機能構造は一切変えない。4択、状況依存で入れ替わる主ボタン、無意識と目的ありの2群、localStorage、通行証。すべて現状のまま。視覚言語だけ置き換える。

---

## 2. 中心となる設計判断 — 明暗の反転を意味に変える

Caldera は暖かく明るい世界観である。ここに正面からぶつかる問題が1つある。

トガメが戦う相手は Instagram と TikTok であり、あれは明るく彩度の高い画面だ。そこから来た指に対して、同じく明るく暖かい画面を出すと、手触りが地続きになって指が同じモードのまま滑る。一拍おかせるという目的が弱くなる。

かといって全画面を暗くすると Caldera ではなくなる。

そこで、Caldera のパレット内で明暗を作り、その落差に意味を持たせる。

- 問い詰め画面は **Obsidian 地に Chalk 文字**。палет内の暗い面である。SNS の明るさから来た瞬間に暗転する
- 理由を選ぶと、その群の色で画面が満ちる（既存モック `dark-mock.html` の演出を流用）
- 返し画面も Obsidian 地のまま。まだ判断の途中だから
- **引き返したら Limestone 地に反転する**。画面が文字通り明るくなる

引き返すという行為の報酬が、明るさそのものになる。バッジもレベルも要らない。それでも開くを選んだ場合は暗いまま SNS へ送り出す。

Caldera の Obsidian と Chalk（暗い面での文字）はパレットに定義されているので、これは逸脱ではなく、暗い面を主戦場に据えるという配分の選択である。

---

## 3. カラー

### 3.1 パレット

```css
--color-ember:         #fc5000;  /* 主アクション・衝動側 */
--color-plasma-violet: #524ae9;  /* 装飾のみ。操作要素には使わない */
--color-sulfur:        #f5f28e;  /* タグ・バッジの地のみ */
--color-limestone:     #f7f6f2;  /* カード面・明るい地 */
--color-pumice:        #e2e2df;  /* キャンバス */
--color-obsidian:      #070607;  /* 文字・境界・暗い地 */
--color-chalk:         #ffffff;  /* 暗い面での文字 */
```

### 3.2 2群4理由をどう塗るか

現状は4理由に4色（赤系2・青系2）。Caldera で操作要素に使える色は Ember しかない（Plasma Violet は装飾専用、Sulfur は明色でコントラストが出ない）。

そこで色の役割を、個体の識別から**群の告発**に変える。

| 理由 | 群 | 塗り |
|---|---|---|
| 暇つぶし | 無意識 | Ember 塗り + Obsidian 文字 |
| 逃避 | 無意識 | Ember 塗り + Obsidian 文字 |
| 調べもの | 目的あり | 透明 + Chalk 1.5px 輪郭 + Chalk 文字 |
| 勉強や仕事 | 目的あり | 透明 + Chalk 1.5px 輪郭 + Chalk 文字 |

暗い画面の中で、無意識側の2つだけが燃えている。目的があるなら色はつかない。**色がつくこと自体が、あなたは今なんとなく開いた、というサインになる**。

現状の4色は、2群という意味を4つに割って薄めていた。2色に減らすことは後退ではなく、意味の純度が上がる。同群内の区別はラベル文字が担う。4択しかないので識別に支障はない。

ダッシュボード（第2段）ではグラフに4値必要なので、Ember 100%/60%、Obsidian 100%/45% の濃淡2段ずつで対応する。

### 3.3 Plasma Violet の置き場所

「ヒーロー装飾のみ、操作不可」という制約に従い、使用箇所は1つだけ。**引き返した画面の背景ハーフトーンドット**。明るい Limestone 地に Plasma Violet のドットが薄く敷かれる。アプリ内でここにしか出ない色なので、引き返した瞬間の特別さが視覚的に立つ。

### 3.4 コントラスト検証

実測値（WCAG相対輝度で算出）。

| 前景 / 背景 | 比 | 判定 |
|---|---|---|
| Chalk #ffffff / Obsidian #070607 | 約 20.4:1 | 全用途で可 |
| Obsidian #070607 / Ember #fc5000 | 約 5.9:1 | 本文可 |
| Ember #fc5000 / Obsidian #070607 | 約 5.9:1 | 本文可 |
| Obsidian #070607 / Limestone #f7f6f2 | 約 18.4:1 | 全用途で可 |
| Obsidian #070607 / Sulfur #f5f28e | 約 15.6:1 | 全用途で可 |
| **Ember #fc5000 / Limestone #f7f6f2** | **約 3.1:1** | **本文不可・24px以上のみ** |
| **Sulfur #f5f28e / Limestone #f7f6f2** | **約 1.2:1** | **文字色に使用禁止** |

守るべき規則は2つ。**Ember を明色地の上で文字色に使う場合は24px以上に限る**。**Sulfur は必ず地色として使い、その上に Obsidian を置く**。

---

## 4. タイポグラフィ

### 4.1 見出し書体の問題

元仕様書は PP Neue Corp Compact 相当として Bebas Neue / Anton / Druk Wide Bold を挙げているが、**この3書体はいずれもラテン文字専用で、日本語グリフを一切持たない**。「なんで開いたの？」に適用すると全文字がフォールバックし、48px 極太コンデンスという指定の核心が日本語画面では成立しない。

トガメの見出しはすべて日本語である。ここは決めないと先へ進めないので、選択肢を3つ出す。

**案A: Noto Sans JP 900 + 横方向を圧縮**
Google Fonts。900 は十分に極太。`transform: scaleX(0.92)` で擬似的にコンデンスへ寄せる。Caldera の圧の強さに最も近い。日本語のウェイト別サブセットが配信されるので実測40〜60KB程度。

**案B: Dela Gothic One**
Google Fonts の日本語ディスプレイ書体。超極太で個性が強く、印象に残らないという不満に直接効く。コンデンスではなく、やや丸みがある。単一ウェイトのみ。

**案C: webfont を使わず system-ui の 800 + scaleX**
外部依存ゼロ。README の依存ライブラリなし単一ファイルという構成を完全に守れる。ただし iOS ではヒラギノになるため、Caldera の極太感は出ない。

推奨は**案A**。理由は、印象に残らないという不満への回答が見出しの圧に懸かっており、そこを system font で妥協すると今回のリデザインの主目的が達成できないため。ただし後述の読み込み問題への対処が前提となる。

### 4.2 本文書体

DM Sans 500 固定。ただし DM Sans も日本語を持たないので、日本語は次の書体へ落ちる。

```css
--font-body: 'DM Sans', -apple-system, BlinkMacSystemFont,
             'Hiragino Sans', 'Yu Gothic UI', sans-serif;
```

英数字は DM Sans、日本語はヒラギノという混植になる。トガメの本文には `INSTAGRAM` `約12分` `今日3回目` など英数字が頻繁に混ざるので、この混植は実際に効く。ウェイトは 500 固定、400 と 700 は使わない。

### 4.3 サイズ

| 用途 | サイズ | 書体 |
|---|---|---|
| 問い詰めの問い | `clamp(38px, 11.5vw, 52px)` | display |
| 返しの一言 | `clamp(30px, 9vw, 42px)` | display |
| 引き返しの見出し | `clamp(24px, 7vw, 30px)` | display |
| 選択肢ラベル | 18px | body 500 |
| 本文・補足 | 16px | body 500 |
| 文脈行（今日3回目…） | 14px | body 500 |
| ラベル（INSTAGRAM を…） | 12px / 字間 0.16em | body 500 |

見出しの字間は +0.02em。日本語は `word-break: auto-phrase` で文節改行させる（現状コードで既に使用中）。

390px 幅では問いが2行に割れるが、文節境界で折れるので破綻しない。

### 4.4 読み込みの問題

介入画面は iOS ショートカット経由で1日に何十回も、しかも**即座に**出る必要がある。webfont が未読み込みの瞬間に表示されると、無表示（FOIT）か差し替え時のガタつき（CLS）が起きる。

対処は3点。

1. `font-display: swap` を指定し、無表示を避ける
2. `<link rel="preconnect">` を `fonts.googleapis.com` と `fonts.gstatic.com` の両方に張る
3. フォールバック側に `size-adjust` を効かせた `@font-face` を定義し、差し替え時のズレを抑える

2回目以降はブラウザキャッシュに載るので、実質的に問題になるのは初回のみ。

### 4.5 依存構成への影響

webfont を入れると、README にある「依存ライブラリなし・単一ファイル」は厳密には崩れる。ライブラリではないので嘘にはならないが、**外部ネットワークへの依存は増える**。オフラインや通信が細い状況で見出しがフォールバックする。許容するかどうかは判断が要る（後述の質問1）。

---

## 5. 形状と余白

### 5.1 角丸

| 要素 | 角丸 |
|---|---|
| カード・選択肢 | 40px |
| 矩形ボタン | 40px |
| ピル型ボタン・タグ | 800px |
| 入力欄 | 100px |
| 小要素 | 16px |

40px は Caldera の署名なので死守する。ここを削ると別物になる。

### 5.2 余白のモバイル調整

元仕様書のカード内パディング 40px は、デスクトップ由来の数値である。390px 幅で適用すると次のようになる。

```
390px - ページ余白24px×2 = 342px（カード幅）
342px - カード内40px×2 = 262px（本文幅）
```

262px に 18px の選択肢ラベルを置くと窮屈で、40px 角丸の内側に文字が寄って見える。次のように調整する。

| 変数 | 480px以上 | 480px未満 |
|---|---|---|
| ページ左右余白 | 24px | 16px |
| カード内パディング | 40px | 28px |
| 要素間ギャップ | 16px | 16px |

480px未満では `390 - 16×2 - 28×2 = 302px` が本文幅になる。40px 角丸との釣り合いが取れる下限がここ。

### 5.3 影

一切使わない。現状コードの `box-shadow` はすべて削除する。階層は色面のコントラストと角丸だけで作る。

### 5.4 タップ領域

Caldera の見た目に関わらず、**タップ判定は最低 48px、主ボタンは 56px を確保する**。1日に何十回も押す画面であり、ここは見た目より優先される。

---

## 6. 画面別仕様

### 6.1 問い詰め（`renderIntercept`）

地は Obsidian。カードは置かず、画面全体を1枚の暗い面として使う。

```
[ラベル]  INSTAGRAM を開こうとしています
          12px / 字間0.16em / Chalk 55%

[問い]    なんで開いたの？
          clamp(38px,11.5vw,52px) / display / Chalk
          文節改行 / 下余白 36px

[選択肢]  ┌────────────────────────┐
          │  暇つぶし              │  Ember塗り / Obsidian文字
          └────────────────────────┘  角丸40px / 内28px / 最小60px
          ┌────────────────────────┐
          │  逃避                  │  Ember塗り / Obsidian文字
          └────────────────────────┘
          ┌────────────────────────┐
          │  調べもの              │  透明 / Chalk1.5px輪郭 / Chalk文字
          └────────────────────────┘
          ┌────────────────────────┐
          │  勉強や仕事            │  透明 / Chalk1.5px輪郭 / Chalk文字
          └────────────────────────┘
          間隔16px

[文脈]    今日3回目。ここまでInstagramだけで18分、SNS全体で41分（推定）。
          14px / Chalk 45% / 上余白 26px
```

現状の色ドットは廃止する。カード全体が色を持つので、ドットは冗長になる。

押下時は `transform: scale(0.975)` のみ。影がないので沈み込みは表現しない。

### 6.2 遷移演出

理由を押すと、その群の色が下から画面を満たす。既存の `dark-mock.html` の実装をそのまま使う。

- 無意識（暇つぶし・逃避）→ Ember が満ちる
- 目的あり（調べもの・勉強や仕事）→ Limestone が満ちる

満ちる色は `linear-gradient(to top, 色, color-mix(in srgb, 色 40%, 地色))` で上を地色へ落とす。ベタ塗りは1日30回では目に強すぎる。

所要 340ms。`--ease-drawer: cubic-bezier(0.32, 0.72, 0, 1)`。`prefers-reduced-motion` では不透明度のみの120msに落とす。

ここで起きているのは演出ではなく告白である。Ember が満ちるのは少し気まずく、Limestone が満ちるのは静かに済む。その一瞬の感情が一拍になる。

### 6.3 返し（`renderFollowup`）

地は Obsidian のまま。まだ判断の途中だから明るくしない。

```
[理由タグ]  暇つぶし
            Sulfur地 / Obsidian文字 / ピル800px / 12px / 内6px12px

[一言]      暇なら、それは自由時間ってこと。
            clamp(30px,9vw,42px) / display / Chalk
            ※AIの一言が入る場合もここ。1〜3行に伸びる想定

[換算]      いつもの1回ぶん（約12分）があれば、本を1章読むことができる。
            16px / Chalk 50%

[主ボタン]  ┌────────────────────────┐
            │      やめておく        │  Ember塗り / Obsidian文字
            └────────────────────────┘  ピル800px / 内12px24px / 最小56px

[副ボタン]        それでも開く
                  下線 / Chalk 45% / 15px / 最小44px
```

主従は状況依存の現状ロジックを維持する。無意識側の理由ならやめておくが主ボタン、目的あり側の理由ならそれでも開くが主ボタンになる。**主ボタンは常に Ember ピル、副ボタンは常に下線テキスト**とし、色ではなく形で主従を示す。

AI の一言は長さが読めないので、`min-height` を確保して読み込み中に高さが飛ばないようにする。読み込み中の点滅表示は現状のものを踏襲し、色を Chalk 30% にする。

### 6.4 引き返した（`renderResistDone`）

**ここで地が Limestone に反転する**。Plasma Violet のハーフトーンドットを背景に敷く。アプリ内で唯一 Plasma Violet が出る場所。

```
（背景全面に Plasma Violet のドット、不透明度8%、間隔24px）

[回数]      3
            display / clamp(64px,20vw,88px) / Ember

[記録]      ● ● ● ○ ○
            今日開いた回数ぶんの丸。引き返した数だけ Ember で塗る
            11px / 間隔7px / 未達は Obsidian 15%

[見出し]    引き返した。今日3回目のえらい。
            clamp(24px,7vw,30px) / display / Obsidian

[補足]      このままホーム画面に戻ろう。
            16px / Obsidian 60%

[リンク]    今日のまとめを見る →
            下線 / Obsidian 55% / 15px
```

バッジもレベルも連続記録も置かない。連続記録は途切れた日に人を辞めさせるので、長く使わせたいなら入れてはいけない。積み上がるのは事実だけでよい。

入場は 260ms の rise。暗→明の反転自体が報酬なので、追加の祝祭演出は不要。

### 6.5 素通り（`renderGrace`）

地は Obsidian。「さっき答えたばかり。」を返し画面と同じ見出し扱いで置き、補足を Chalk 45% で1行。ボタンは置かない。最も頻度が高くなり得る画面なので、最も静かにする。

---

## 7. CSS 変数

```css
:root{
  /* Caldera パレット */
  --color-ember:#fc5000;
  --color-plasma-violet:#524ae9;
  --color-sulfur:#f5f28e;
  --color-limestone:#f7f6f2;
  --color-pumice:#e2e2df;
  --color-obsidian:#070607;
  --color-chalk:#ffffff;

  /* 役割への割当 */
  --ground-dark:var(--color-obsidian);
  --ground-light:var(--color-limestone);
  --canvas:var(--color-pumice);
  --ink-on-dark:var(--color-chalk);
  --ink-on-light:var(--color-obsidian);
  --impulse:var(--color-ember);      /* 無意識側 */
  --deliberate:var(--color-chalk);   /* 目的あり側は輪郭のみ */

  /* 書体 */
  --font-display:'Noto Sans JP','Hiragino Sans',sans-serif;
  --font-body:'DM Sans',-apple-system,BlinkMacSystemFont,
              'Hiragino Sans','Yu Gothic UI',sans-serif;

  /* 角丸 */
  --radius-cards:40px;
  --radius-buttons:40px;
  --radius-pills:800px;
  --radius-inputs:100px;
  --radius-small:16px;

  /* 余白 */
  --page-pad:24px;
  --card-padding:40px;
  --element-gap:16px;

  /* 動き（既存コードから継承） */
  --ease-out:cubic-bezier(0.23,1,0.32,1);
  --ease-drawer:cubic-bezier(0.32,0.72,0,1);
}

@media (max-width:479px){
  :root{
    --page-pad:16px;
    --card-padding:28px;
  }
}
```

---

## 8. 実装上の注意

現状コードから**削除**するもの。

- `box-shadow` の全指定（影なしの原則）
- `backdrop-filter` を使った半透明ナビ（Caldera はフラット）
- `border-radius` の 10px / 12px / 14px 系（40px と 16px の2値に集約）
- 理由の色ドット `.dot`（カード全体が色を持つため冗長）
- `--r-hima` `--r-tohi` `--r-shirabe` `--r-work` の4色（2群に集約）

現状コードから**維持**するもの。

- `word-break: auto-phrase` による文節改行
- 入場アニメの強度の出し分け（問い詰めは150msフェード、それ以外は250msのrise）
- `prefers-reduced-motion` と `prefers-reduced-transparency` の分岐
- `@starting-style` による入場
- 通行証（クリップボード）まわりの一切
- localStorage のスキーマ

進め方は、既存の `vb-mock.html` `dark-mock.html` と同じくモックを別ファイル（`caldera-mock.html`）で先に作り、`index.html` には触れない。納得してから本体へ流し込む。

---

## 9. 未決事項

実装前に決める必要がある。

1. **webfont を許容するか**。Noto Sans JP を入れると外部ネットワーク依存が発生する。README の依存なし単一ファイルという構成を優先するなら案C（system font + 横圧縮）になるが、Caldera の圧は出ない。
2. **見出し書体の選択**。案A（Noto Sans JP 900 + 横圧縮／推奨）、案B（Dela Gothic One）、案C（system font）。
3. **問い詰め画面を Obsidian 地にする案の可否**。Caldera の明るい世界観からは外れるが、落差で一拍を作る機構は維持できる。全画面 Limestone 基調で通す判断もあり得る。
4. **理由の色を2色に減らす案の可否**。4色から2色になるが、2群という意味は強くなる。
5. **スコープ**。介入4画面のみか、ダッシュボードと設定まで一気に広げるか。
