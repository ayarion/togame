# トガメ ショートカット設定ガイド（通行証方式）

「それでも開く → SNSに戻る → またトガメが開く」の無限ループを止める設定。

## 仕組み

トガメで「それでも開く」を押すと、アプリがクリップボードに **`togame-pass`**（通行証）を書いてからSNSに戻る。
オートメーションは最初にクリップボードを確認して：

- **通行証がある** → 通行証を消して、何もしない（＝SNSがそのまま使える。ループが切れる）
- **通行証がない** → 普通にトガメを開く

ファイルも日付計算も使わない。**全アプリ同じ内容**で作れる（変えるのは対象アプリと最後のURLだけ）。

---

## オートメーションの作り方（Instagramの例）

### 下準備
1. ショートカットアプリ → 下タブ「オートメーション」→ 右上「＋」
2. 「App」を選ぶ →「開いている」にチェック → **Instagram** を選択 →「次へ」
3. **「すぐに実行」を選ぶ**（「実行前に尋ねる」だと毎回バナーが出る）

### アクション（上から順に5個）

カッコ内は英語表示のときの名前。

1. **クリップボードを取得**（Get Clipboard）
   - 設定不要。今のクリップボードの中身を読む

2. **if**（If）
   - 条件：「クリップボード」**が次を含む**（contains）→ `togame-pass` と入力
   - 【**その場合**】に 3〜4 を入れる。【**その他の場合**】に 5 を入れる

3. （その場合の中）**テキスト**（Text）
   - `済` とだけ入力（通行証を消すための上書き用。文字はなんでもいい）

4. （その場合の中）**クリップボードにコピー**（Copy to Clipboard）
   - 入力：手順3の「テキスト」
   - → 通行証が消える。**ここで終わり＝何もしない＝Instagramがそのまま前面に残る**

5. （その他の場合の中）**URLを開く**（Open URLs）
   - トガメの「設定 → 監視するアプリ」で **Instagram用のURLをコピー** して貼る
     （`https://ayarion.github.io/togame/?app=instagram` の形）

6. 右上「完了」で保存

### 動作確認
Instagramを開く → トガメが出る → 理由を選ぶ →「それでも開く」→ Instagramが開いて、**そのまま使える**（トガメに戻らなければ成功）。

---

## 他のアプリ（X / TikTok / YouTube）

同じものを作って、変えるのは2箇所だけ：
- トリガーの対象アプリ
- 手順5のURL（トガメの設定でそのアプリ用をコピー）

手順1〜4は完全に同じ。

---

## 知っておくといいこと

- **クリップボードが1回上書きされる**：「それでも開く」を押すと、直前にコピーしていたものが `togame-pass` に置き換わる。何かコピー中に押した場合はコピーし直しになる（実害はほぼない）
- **初回だけ許可を求められることがある**：「クリップボードへのアクセスを許可しますか」的な確認が出たら許可する
- **5分以内にもう一度開いた場合**：通行証は使い切りなので、猶予時間内でもトガメの「さっき答えたばかり」画面が一度挟まる。「Instagramに戻る」を押せば（また通行証が発行されて）すぐ戻れる

## うまくいかないとき

- **毎回確認バナーが出る** → オートメーションが「実行前に尋ねる」になっている。「すぐに実行」に変更
- **ループが直らない** → 手順2の条件が「**が次を含む**（contains）」で、値が `togame-pass`（半角・ハイフン込み）になっているか確認
- **トガメ自体が開かない** → 手順5が【その他の場合】の中に入っているか確認（ifの外や【その場合】に入っていると動かない）

詰まったら、オートメーション全体のスクショを送ってくれれば一緒に直せる。
