# トガメ ショートカット設定ガイド（無限ループ対策版）

「それでも開く → SNSに戻る → またトガメが開く」の無限ループを止めるための、
iPhoneショートカットの組み方。アプリ側ではこのループを止められない（SNSを開くたび
iOSのオートメーションが問答無用で再発火するため）ので、**ショートカット側に
「さっき質問したばかりなら何もしない」というクールダウン判定**を入れて解決する。

対象は Instagram の例。他アプリは末尾の「複製」を参照。

---

## 仕組み（概念）

SNSを開くと、オートメーションがまず「前回トガメで質問した時刻」を記録ファイルから読む。

- **前回の質問から5分以内** → 何もしない（＝Instagramが前面に残る。ループが切れる）
- **5分より前 or 初回** → 今の時刻を記録して、トガメを開く

この「5分」はトガメの設定「引き止めない時間」と同じ意味。**両方同じ数字にそろえる**こと。

---

## パートA: Instagram用オートメーション

### 下準備
- ショートカットアプリ → 下タブ「オートメーション」→ 右上「＋」→「新規オートメーション」
- 「App」を選ぶ →「開いている」にチェック → アプリで **Instagram** を選択 → 「次へ」
- **「すぐに実行」を選ぶ**（「実行前に尋ねる」はオフ。これがオンだと毎回バナーが挟まる）

### アクションを上から順に追加

以下のアクションを、この順番で並べる。カッコ内は英語名（iOSの言語設定で表示が変わるため）。

1. **現在の日付**（Current Date）
   - 何も設定しなくてOK。これが「今」になる。

2. **日付を調整**（Adjust Date）
   - 入力に「現在の日付」を指定
   - 「**減算**」を選び、「**5**」「**分**」にする
   - これが「基準時刻（5分前）」になる

3. **ファイルを取得**（Get File）
   - サービス：**iCloud Drive**
   - パス：`Shortcuts/togame-last-instagram.txt`
   - 詳細を開き、「**見つからない場合は停止**（Error If Not Found）」を**オフ**にする
     （初回はファイルが無いので、止まらず先に進ませるため）

4. **入力から日付を取得**（Get Dates from Input）
   - 入力に「ファイルを取得」の結果を指定
   - ファイルに保存してある前回の質問時刻を、日付として取り出す

5. **if**（If）
   - 条件：「入力から日付を取得」**が** 「次より後」（is after）**基準時刻**
   - ※「基準時刻」は手順2「日付を調整」の結果を指定
   - **【その場合】＝前回の質問が5分以内**
     - **ここには何も入れない**（空のまま）。→ 何もせずInstagramが前面に残る＝ループが切れる
   - **【その他の場合】＝5分より前 or 初回** … 下の6〜8をこの中に入れる

6.（その他の場合の中）**テキスト**（Text）
   - 内容に「**現在の日付**」の変数を挿入（手順1のもの）。ほかには何も書かない。

7.（その他の場合の中）**ファイルを保存**（Save File）
   - 入力：手順6の「テキスト」
   - サービス：**iCloud Drive**、保存先：`Shortcuts/` フォルダ、ファイル名：`togame-last-instagram.txt`
   - 「**保存時に尋ねる**（Ask Where to Save）」を**オフ**
   - 「**上書き**（Overwrite If File Exists）」を**オン**
   - → 今の時刻で記録ファイルを上書きする

8.（その他の場合の中）**URLを開く**（Open URLs）
   - トガメアプリの「設定 → 監視するアプリ」で **Instagram用のURLをコピー**して、ここに貼る
     （`https://ayarion.github.io/togame/?app=instagram` の形）

9. 右上「完了」で保存

### 動作確認
Instagramを一度閉じて開き直す → トガメが出る → 理由を選ぶ →「それでも開く」→ Instagramが開く
→ **今度はトガメに戻らず、Instagramのまま使える**。5分たってからもう一度Instagramを開くと、また質問が出る。

---

## 他アプリへ複製

1. オートメーションタブで、作ったInstagramの自動化を開く
2. 同じ手順でアプリだけ X / TikTok / YouTube に変えて作り直す
3. 変える箇所は2つだけ：
   - 手順3と7の**ファイル名**を、アプリごとに別にする
     （例：`togame-last-x.txt`, `togame-last-tiktok.txt`）。ここを共通にすると誤作動する
   - 手順8の**URL**を、そのアプリ用のもの（トガメ設定でコピー）に変える

---

## うまくいかないときの切り分け

- **毎回バナーが出て自動で開かない** → 「すぐに実行」がオフになっている。オンに。
- **初回で止まる/エラー** → 手順3「見つからない場合は停止」がオンのまま。オフに。
- **ずっとトガメに戻ってしまう（ループが直らない）** → 手順5のif条件が「次より後（is after）」に
  なっているか、参照先が「基準時刻」になっているかを確認。逆向き（前 / before）だと動作が反転する。
- **日付がうまく比較できない** → 手順6のテキストに入れるのは「現在の日付」の変数のみ。文字を足さない。
  それでも不安定なら、手順6でテキスト変数をタップして日付フォーマットを固定できる（例 ISO 8601）。

※アクション名の表示はiOSのバージョン・言語で少し変わる。見つからないときは「意味」で近いものを探す。
不明点があれば、詰まった手順番号とスクショを送ってくれれば一緒に直せる。
